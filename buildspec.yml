version: 0.1

phases:
  pre_build:
    commands:
      - npm install
      - npm test
  build:
    commands:
      - npm run webpack
artifacts:
  files:
    - index.html
    - styles/main.css
    - images/*
    - pdf/*
    - dist/bundle.js
